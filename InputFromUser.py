from typing import re

u=int(input('Please enter no'))
x=int(input('Please enter no'))
z=u+x
print(z)

# /Note : input method will by defualt take the String value, so we have to convert the String into Integer format



# If we need to print the character we can use the input function but input function take string as a parameter, so here we can restrict the String to first character by using[0]

ch=input("enter the character")[0]
print(ch)

#  Below code is used to evaluate an expression
result =eval(input("Enter the expression"))
print(result)


